import React from 'react';
import { Link } from 'react-router-dom';
import TopNav from './TopNav';

const Projects = () => {
    return (
        <div>
            <TopNav projectsPage={true} />
        </div>
    );
};

export default Projects;